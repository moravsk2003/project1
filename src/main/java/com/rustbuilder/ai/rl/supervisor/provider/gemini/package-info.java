/**
 * Gemini provider adapter for the RL supervisor boundary.
 *
 * This package owns Gemini prompts, request bodies, HTTP calls, response parsing,
 * and primary/fallback model selection. It must not orchestrate training runs.
 */
package com.rustbuilder.ai.rl.supervisor.provider.gemini;
